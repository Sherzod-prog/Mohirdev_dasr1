'use strict'

const number = {
    x: 10,
    y: 5
};

const addNumbers = {
    z: 18,
    w: {
        a: 22,
        b: 14
    }
};

// const allNumbers = Object.assign(number, addNumbers)

// allNumbers.w.a = 44
// addNumbers.w.a = 50

console.log(number);
console.log(addNumbers);
// console.log(allNumbers);